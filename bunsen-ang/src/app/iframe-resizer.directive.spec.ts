import { IFrameResizerDirective } from './iframe-resizer.directive';

describe('IFrameResizerDirective', () => {
  it('should create an instance', () => {
    const directive = new IFrameResizerDirective();
    expect(directive).toBeTruthy();
  });
});
